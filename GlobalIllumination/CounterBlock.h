#pragma once
#include <glad/glad.h>

struct CounterBlock {
    GLuint fragmentCounter;
    GLuint nodeCounter;
    GLuint brickCounter;
    GLuint leafCounter;
    GLuint logCounter;
    GLuint noOfFragments;
};